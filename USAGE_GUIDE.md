# 動画チェックツール 使い方ガイド

このガイドでは、知識がない方でも簡単にアプリを使えるように、ステップバイステップで説明します。

## 方法1: ブラウザで使う（最も簡単・推奨）

Vercelにデプロイされている場合は、ブラウザで直接使えます。

### ステップ1: アプリにアクセス

1. ブラウザ（Chrome、Firefox、Safariなど）を開く
2. 以下のURLにアクセス：
   - Vercelにデプロイされている場合: `https://movie-check-xxxxx.vercel.app`
   - または、GitHubリポジトリのREADMEに記載されているURL

### ステップ2: ファイルをアップロード

1. 画面上部に4つのファイルアップロード欄があります：
   - **Premiere Pro XML**: タイムライン情報を含むXMLファイル
   - **チェック対象動画 (MP4)**: チェックしたい動画ファイル
   - **テロップテキスト (TXT)**: タイムコード付きのテキストファイル（オプション）
   - **お手本動画 (MP4)**: 音量比較用の参考動画（オプション）

2. 各欄にファイルをドラッグ&ドロップするか、クリックしてファイルを選択

### ステップ3: チェックを実行

1. 「チェック開始」ボタンをクリック
2. 処理が完了するまで待つ（進捗バーで進行状況を確認できます）
3. 結果がテーブルに表示されます

### ステップ4: 結果を確認・保存

1. 検出されたエラーがテーブルに表示されます
2. 「CSV出力」ボタンをクリックして、結果をCSVファイルとしてダウンロード

---

## 方法2: 自分のPCでアプリを起動する

自分のPCでアプリを起動して使いたい場合の手順です。

### 必要なもの

- **PC**: Windows、Mac、Linuxのいずれか
- **インターネット接続**: 初回セットアップ時に必要

### ステップ1: Node.jsをインストール

1. [Node.jsの公式サイト](https://nodejs.org/)にアクセス
2. 「LTS」と書かれた緑色のボタンをクリック（推奨版）
3. ダウンロードされたファイルを実行
4. インストールウィザードに従ってインストール
   - 「Next」や「Install」をクリックしていくだけ
   - デフォルト設定で問題ありません
5. インストールが完了したら、PCを再起動（推奨）

### ステップ2: アプリのファイルをダウンロード

#### 方法A: GitHubからダウンロード（推奨）

1. [GitHubリポジトリ](https://github.com/あなたのユーザー名/Movie-check)にアクセス
2. 緑色の「Code」ボタンをクリック
3. 「Download ZIP」をクリック
4. ダウンロードしたZIPファイルを解凍
5. 解凍したフォルダを開く

#### 方法B: Gitを使う（上級者向け）

```bash
git clone https://github.com/あなたのユーザー名/Movie-check.git
cd Movie-check
```

### ステップ3: アプリをセットアップ

1. 解凍したフォルダ（または`Movie-check`フォルダ）を開く
2. フォルダ内で右クリック → 「ターミナルで開く」（Mac）または「PowerShellで開く」（Windows）
   - または、コマンドプロンプトやターミナルを開いて、`cd`コマンドでフォルダに移動

3. 以下のコマンドを入力してEnterキーを押す：

```bash
npm install
```

4. インストールが完了するまで待つ（数分かかることがあります）
   - 「added XXX packages」と表示されれば完了

### ステップ4: アプリを起動

1. 同じターミナル（コマンドプロンプト）で、以下のコマンドを入力：

```bash
npm run dev
```

2. 以下のようなメッセージが表示されます：

```
  VITE v7.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

3. ブラウザを開いて、`http://localhost:5173` にアクセス
   - 自動的にブラウザが開く場合もあります

### ステップ5: アプリを使う

1. ブラウザでアプリが表示されます
2. 「方法1」のステップ2以降と同じ手順で使えます

### アプリを終了する

1. ターミナル（コマンドプロンプト）で `Ctrl + C`（Macの場合は `Cmd + C`）を押す
2. アプリが停止します

---

## ファイル形式について

### Premiere Pro XMLファイル

1. Premiere Proでプロジェクトを開く
2. 「ファイル」→「書き出し」→「Final Cut Pro XML」を選択
3. 保存場所を指定して保存

### テキストファイル（TXT）

以下の形式で記述してください：

```
00:00:01:00 こんにちは
00:00:05:15 これはテストです
00:00:10:30 誤字脱字をチェックします
```

各行は `HH:MM:SS:FF テキスト内容` の形式で記述します。

---

## よくある質問

### Q: 「npm install」でエラーが出る

A: 以下を確認してください：
- Node.jsが正しくインストールされているか（`node --version`で確認）
- インターネット接続が正常か
- フォルダのパスに日本語や特殊文字が含まれていないか

### Q: ブラウザで「接続できません」と表示される

A: 以下を確認してください：
- `npm run dev`が正常に実行されているか
- 正しいURL（`http://localhost:5173`）にアクセスしているか
- ファイアウォールがブロックしていないか

### Q: チェックが完了しない

A: 以下を確認してください：
- ファイルサイズが大きすぎないか（動画ファイルは特に注意）
- ブラウザのコンソールにエラーが表示されていないか（F12キーで確認）
- インターネット接続が正常か（誤字脱字チェックにはAPI接続が必要）

### Q: 誤字脱字チェックが動作しない

A: 以下を確認してください：
- Vercelにデプロイされている場合、環境変数`GEMINI_API_KEY`が設定されているか
- ローカルで実行している場合、`vercel dev`を実行してAPIを起動する必要があります

---

## トラブルシューティング

### Node.jsがインストールされているか確認

ターミナル（コマンドプロンプト）で以下を実行：

```bash
node --version
```

バージョン番号が表示されれば、インストールされています。

### npmがインストールされているか確認

```bash
npm --version
```

バージョン番号が表示されれば、インストールされています。

### アプリのフォルダに移動

```bash
cd /path/to/Movie-check
```

Windowsの場合：
```bash
cd C:\Users\あなたのユーザー名\Downloads\Movie-check
```

---

## サポート

問題が解決しない場合：
1. エラーメッセージをコピー
2. GitHubのIssuesに投稿
3. または、READMEに記載されている連絡先に問い合わせ
